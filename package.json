{
  "name": "pwa-workbox-precache-nuxt",
  "version": "1.0.0",
  "description": "Precache all nuxt files with workbox-build",
  "author": "matt blewitt",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "build": "nuxt build && npm run buildSw && nuxt build",
    "start-pwa": "npm run build && nuxt start",
    "buildSw": "node ./pwa/buildSw"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "workbox-build": "^5.1.3"
  },
  "devDependencies": {}
}
